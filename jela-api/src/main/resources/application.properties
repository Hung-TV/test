spring.application.name=jela-api

# =====================
# Database (PostgreSQL)
# =====================
#spring.datasource.url=${JELA_DB_URL:jdbc:postgresql://localhost:5432/JELA_Japanese-Education-Learning-App?reWriteBatchedInserts=true}
#spring.datasource.username=${JELA_DB_USERNAME:postgres}
#spring.datasource.password=${JELA_DB_PASSWORD:12345}
#spring.datasource.driver-class-name=org.postgresql.Driver
# =====================
# Database (PostgreSQL_Render)
# =====================
spring.datasource.url=${JELA_DB_URL:jdbc:postgresql://dpg-d9a3q1l7vvec738hldr0-a.ohio-postgres.render.com:5432/jela_db?sslmode=require&reWriteBatchedInserts=true}
spring.datasource.username=${JELA_DB_USERNAME:jela_admin}
spring.datasource.password=${JELA_DB_PASSWORD:K9tZtEmREl6LgvmA3MXXrhUitHqoWhb2}
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP Performance Optimizations for PostgreSQL Batch inserts
spring.datasource.hikari.data-source-properties.reWriteBatchedInserts=true
spring.datasource.hikari.data-source-properties.cachePrepStmts=true
spring.datasource.hikari.data-source-properties.prepStmtCacheSize=250
spring.datasource.hikari.data-source-properties.prepStmtCacheSqlLimit=2048
spring.datasource.hikari.data-source-properties.useServerPrepStmts=true

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=false

# =====================
# Flyway
# =====================
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
# Sua loi checksum khi file migration da chay bi thay doi
spring.flyway.validate-on-migrate=false
spring.flyway.repair-on-migrate=true

# =====================
# Startup data import
# =====================
# Duong dan den thu muc data-import, de trong se tu dong tim o thu muc goc cua du an
app.data-import.dir=../data-import

# =====================
# Security / JWT
# =====================
app.jwt.secret=${JELA_JWT_SECRET:ZGV2LXNlY3JldC1jaGFuZ2UtbWUtcGxlYXNlLWxvbmc}
app.jwt.access-token-ttl-seconds=${JELA_JWT_ACCESS_TTL:900}
app.jwt.refresh-token-ttl-days=${JELA_JWT_REFRESH_TTL_DAYS:30}

# CORS (comma-separated) - Allowing both 5173 and 5174
app.cors.allowed-origins=${JELA_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:5174}

# =====================
# Google OAuth2
# =====================
spring.security.oauth2.client.registration.google.client-id=962773257163-l6fdpi0tn77tbg5305obmflcjn00kuef.apps.googleusercontent.com
spring.security.oauth2.client.registration.google.client-secret=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
# Spring Mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=tatvinhhung2004@gmail.com
# Note: this is a Google App Password, not the normal email login password.
spring.mail.password=kfgxswamddforkth
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# =====================
# Client URL
# =====================
client.url=${JELA_CLIENT_URL:http://localhost:5173}